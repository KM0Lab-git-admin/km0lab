/**
 * @km0lab/app — data
 *
 * Re-exports de los data compartidos entre apps. Añadir aquí los data nuevos
 * importándolos desde su archivo y exportándolos.
 *
 * Convención: un archivo por data (kebab-case para ficheros de utils/data,
 * `useXxx.ts` para hooks).
 */

export {}
